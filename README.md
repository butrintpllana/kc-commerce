# Food Ordering App

Technical challenge project: PHP/Laravel + React + MySQL.

## Structure
- `/backend` — Laravel REST API
- `/frontend` — React SPA

## Setup
(instructions coming as each part is built)